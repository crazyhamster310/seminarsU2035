1. Написать программу, использующую класс `ROSInterface`, (пример такой программы находится в файле `main.py` данного семинара) которая заставляет делать дрон следующее:
    1. Взлет на 4.0 относительно земли
    2. Пролет в точку `(x, y, z) = (10.0, 20.0, 3.0)`
    3. Пролет в точку `(x, y, z) = (30.0, 20.0, 1.0)`
    4. Возврат в исходную точку (на высоте 4.0)

**Примечание 1**: для сохранения исходной позиции удобно использовать метод `get_odometry`

**Примечание 2**: не забывайте в конце вызывать метод `shutdown` 

**Результат**: Файл с кодом программы (допускается также снимок экрана, на котором видно код)