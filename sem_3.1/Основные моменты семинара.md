1. Установка `ROS 2 Humble` и `Gazebo Fortress`
2. Команда `ign` и ее подкоманды
3. Графический интерфейс `gazebo`
   1. Верхнее меню
   2. Инструменты для работы с объектами
   3. Запуск симуляции
   4. Инспектор объектов и дерево сущностей
   5. Перемещение и вращение объектов
4. Топики и сервисы `gazebo` (вывод, прослушивание и информация) (`ign topic/service`)
5. Типы данных в `gazebo` (`ign msg`)
6. SDF \- `link`, `collision`, `visual`, `inertial`, `sensor`, `plugin`
7. Добавление модели в мир
8. `ros_gz_bridge`
   1. Зачем
   2. `yaml`-файл
